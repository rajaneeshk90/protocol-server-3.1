properties:
  message:
    properties:
      order:
        properties:
          provider:
            required:
              - id
          items:
            minItems: 1
        required:
          - provider
          - items
          - billing